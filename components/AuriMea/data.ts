import { Transaction, TransactionTemplate, Categories } from './types';

export const initialTransactions: Transaction[] = [];

export const initialTemplates: TransactionTemplate[] = [];

export const initialCategories: Categories = {
    "income": [],
    "expense": [],
};
